import 'package:flutter_test/flutter_test.dart';
import 'package:samguk_master/main.dart';

void main() {
  testWidgets('SamgukQuizApp can be constructed', (tester) async {
    expect(const SamgukQuizApp(), isA<SamgukQuizApp>());
  });
}
